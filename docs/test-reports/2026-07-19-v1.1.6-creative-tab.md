# v1.1.6 创造栏与基础品质验证记录

日期：2026-07-19
状态：自动化门禁与客户端创造栏检查通过

## 变更范围

- 「赛博农夫」创造栏图标由光谱单片镜改为酒精花种子。
- 三种基础种子的速度、产量、效价统一为 `5/5/5`，并同步培养配方中的 JEI 默认展示值。
- 正式创造栏移除代数、突变和零散突破活性测试样本。
- 主「赛博农夫」标签精简为 22 个基础物品和品质 5 代表样本。
- 新增「赛博农夫：品质样本」标签：三种种子各提供 `1/1/1` 至 `10/10/10` 的均衡基因样本，品质 `1-10` 各保留一套三种灌装原料，神经莓和三种血清各保留 Activity `1-15` 的完整变体，共 120 个堆栈。
- 品质变体生成从标签注册类提取到独立辅助类，统一控制范围、NBT 和稳定顺序。

## 自动化结果

| 检查 | 结果 |
|------|------|
| `./gradlew compileJava` | PASS |
| `./gradlew runData` | PASS；中英文创造栏名称键保持一致，仅生成预期的「品质样本」翻译 |
| `./gradlew build` | PASS；生成 `build/libs/cybercultivator-1.1.6.jar` |
| `./gradlew runGameTestServer` | PASS；实际执行 18 项，零失败 |
| `git diff --check` | PASS |

新增 GameTest 覆盖三种默认种子的 `5/5/5` 基因值、内置培养配方的 JEI 默认基因值、默认培养产物的品质 5，以及种子、原料、神经莓和血清品质样本的数量、顺序和 NBT 完整性。

## 客户端检查

`./gradlew runClient` 成功完成 OpenGL 4.6、注册表、资源、Curios 和 JEI 初始化，载入开发世界并实际打开「赛博农夫」创造栏：

- 页签显示酒精花种子的紫色花朵图标。
- 主标签仅显示基础设施、基础种子、装备和品质 5 代表样本，共 22 个堆栈。
- 相邻的「赛博农夫：品质样本」标签完整显示标题且不再显示搜索栏；三种种子的均衡基因样本按等级排列，每三个相邻原料构成一套同品质灌装材料，等级与品质 1-10 均存在。
- 品质样本中的神经莓按 Activity 递增排列，三种血清按同 Activity 三件套排列，Activity 1-15 均存在。
- `run/logs/latest.log` 未发现 ERROR 或 FATAL。

客户端检查完成后由终端主动结束，因此 `runClient` 的退出码 130 不代表运行时失败。
