# v1.1.6 机器方块贴图细化测试记录

## 范围

- 保留四台机器既有 16×16 外框、尺寸、朝向与模型引用，不调用图像生成或替换整套贴图。
- 第一轮逐像素细化 8 张独立前面/顶部贴图，以及共用的 `machine_side.png`、`machine_bottom.png`。
- 第二轮只调整正面功能区：培养槽保持不变；基因拼接机最终改为占满观察窗主体的蓝红横向 DNA“扭转梯”，使用连续双链、四组长短递变的碱基横档和中央交叉，并移除样本点；后续细化双链端部高光、交叉遮挡阴影、观察窗冷暖反光及独立底部状态灯，状态灯与 DNA 之间保留完整暗色隔离行；大气冷凝器改为冷凝鳍片、滴水通道和储水槽；血清灌装机整理灌装头、瓶颈及右侧状态灯。

## 拼接机 DNA 参考

- `Genetics: Resequenced` 的 16×16 `dna_helix.png`：提炼宽端、收束交叉、连续双链与规则横档，不复制原图配色或像素排列。
- `Advanced Genetics` 的 DNA 图标：参考高对比双链和清晰横档在小尺寸下的识别方式，不复制原图。

## 检查结果

| 检查 | 结果 | 说明 |
|------|------|------|
| PNG 规格 | 通过 | 10 张贴图均保持 16×16、全不透明，无缩放或抗锯齿 |
| 构图对比 | 通过 | 金属外框和机器朝向未改变；拼接机、冷凝器、灌装机仅重排正面黑色功能窗口内部 |
| 培养槽保护 | 通过 | 第二轮前后 `bio_incubator_front.png` SHA-256 一致 |
| `git diff --check` | 通过 | 无空白错误 |
| `./gradlew build` | 通过 | `BUILD SUCCESSFUL`，生成 `cybercultivator-1.1.6.jar` |
| `./gradlew runClient` | 通过 | Forge 进入主菜单，方块纹理图集成功创建，无缺失资源或模型错误 |

## 未执行项

- 未运行 `runData`：本轮未修改 datagen provider 或生成资源契约。
- 未运行 GameTest：本轮只修改手写 PNG 与贴图说明，不改变注册、配方或状态机。
- 当前会话未暴露开发客户端控制 MCP，因此未自动进入世界摆放四台机器；已通过最近邻放大的修改前后拼图完成静态视觉核对。

## 版本

- `mod_version=1.1.6`，本轮未改版本号。
