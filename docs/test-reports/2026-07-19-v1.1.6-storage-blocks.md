# v1.1.6 硅晶块与稀土块测试记录

## 范围

- 新增 `raw_silicon_block` 与 `raw_rare_earth_block` 的注册、方块物品、创造栏条目、模型、掉落与挖掘标签。
- 粗矿、精炼材料分别拥有储存块，共四组 `9 个材料 -> 1 个储存块 -> 9 个材料` 可逆工作台配方。
- 粗硅晶块与粗稀土块使用碎晶/压实矿团贴图；精炼硅晶块与稀土块按用户指定的模组模板重新配色。

## 自动检查

| 检查 | 结果 | 说明 |
|------|------|------|
| `./gradlew compileJava` | 通过 | 仅保留项目已有的弃用警告 |
| `./gradlew runData` | 通过 | 生成方块状态、模型、掉落表、配方、进度与中文语言资源 |
| 中英文语言键对比 | 通过 | `en_us.json` 与生成的 `zh_cn.json` 键集合一致 |
| `./gradlew build` | 通过 | 资源重绘后重新构建成功 |
| `./gradlew runGameTestServer` | 通过 | 20 项必需测试全部通过；储存块测试覆盖四组压缩与拆解配方 |
| PNG 规格检查 | 通过 | 四张贴图均为 16×16、全不透明；分别使用 7、10、6、8 个实体色 |
| `git diff --check` | 通过 | 无空白错误 |

## 视觉检查

- 粗硅晶块采用青灰碎晶与短折线高光；粗稀土块采用橙褐压实矿团、暗缝与淡金亮点。
- 精炼硅晶块按用户指定的 Thermal 风格内嵌矿物块模板改为青蓝—冰青色板；精炼稀土块按用户指定的 Modern Industrialization 风格烧结块模板改为橙褐—琥珀色板。
- 额外检查 Mekanism 与 Immersive Engineering 的储存块资源，用于确认有限色板、包边强度、角部压暗和内部细节密度。
- `./gradlew runClient` 已进入主菜单，方块纹理图集成功创建，未出现缺失模型或关键资源错误。日志位于 `run/logs/latest.log`。
- 当前会话未暴露开发客户端控制 MCP，因此未自动进入世界执行摆放、快捷栏与创造栏目视检查；该步骤仍建议人工复核。

## 版本

- `mod_version=1.1.6`，本次未改版本号。
